version: 0.0.1
metadata:
  code: APP
  name: app
  description: Configuration generated by restqa init
tests:
  local:
    port: 8081
    command: "npm run dev"

plugins:
  - name: '@restqa/faker-plugin'

